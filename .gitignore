# Node / package manager
node_modules/
.pnp/                 # yarn pnp (if used)
.vscode/
.idea/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Lockfile - 보통은 커밋 권장 (주석 해제하면 무시)
# Uncomment to ignore lockfile (not recommended usually)
# pnpm-lock.yaml
# package-lock.json
# yarn.lock

# Nuxt build / runtime outputs
.nuxt/
.output/              # Nuxt 3/4 빌드 아웃풋
dist/                 # 정적 빌드 출력 (SSG 등)
.runtime/             # (있다면 런타임 출력)

# Local env files (비밀값)
.env
.env.*                # .env.local, .env.production 등 모두 무시
!.env.example         # 템플릿 파일은 커밋 가능

# Generated artifacts / caches
.cache/
.nuxt.cache/
.vite/
.vite/*               # vite 캐시
coverage/
coverage-final.json

# TypeScript / IDE / OS
*.tsbuildinfo
.DS_Store
Thumbs.db

# Test runners / coverage
jest.config.*.cache
.vitest/

# pnpm / workspace
.pnpm-store/
.pnpm-debug.log
packages/*/node_modules/
apps/*/node_modules/

# local build outputs from packages (if you build libs to dist/)
packages/**/dist/
packages/**/.turbo/
packages/**/.parcel-cache/

# Editor temp / OS
*.sublime-workspace
*.sublime-project
*.swp
*.swo

# Deployment / hosting (optional)
.now/
.vercel/
.netlify/
firebase-debug.log

# Optional: log files created by the app
logs/
*.log

# Optional: generated docs
docs/.vitepress/dist/
